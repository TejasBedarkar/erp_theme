// The subject column renders the linked Scheduled Job Type title, so the
// formatter receives the method path rather than the link's document name.
// Search is not bridged: the standard filter matches that document name.

magnaerp.method_path.rebrand_listview("Scheduled Job Log", "scheduled_job_type");
